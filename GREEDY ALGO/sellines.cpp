#include<bits/stdc++.h>
using namespace std;
#define ll long long

int32_t main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

  vector<int>wines{50,30,20,40,100};
    sort(wines.begin(),wines.end());
    for(int i=0;i<wines.size())
}